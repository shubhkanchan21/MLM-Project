# copy this to .env and fill values for your PostgreSQL setup
DB_HOST=localhost
DB_PORT=5432
DB_NAME=your_database
DB_USER=your_user
DB_PASSWORD=your_password

# json web token secret (used by auth endpoints)
JWT_SECRET=change_me
